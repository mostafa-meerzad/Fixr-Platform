{
  "name": "fixr-platform",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.3.12",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "backend": "turbo dev --filter=@fixr/backend",
    "admin": "turbo dev --filter=@fixr/admin",
    "mobile": "turbo dev --filter=@fixr/mobile"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
